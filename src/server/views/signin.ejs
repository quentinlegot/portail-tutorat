<%- include('header', {title: "Page de connexion", style: "signin"}) %>

<div id="page_connexion">
    <h1>Connexion</h1>

    <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit. In nec nibh vitae...</p>

    <%=error%>
</body>
        <% if(session === undefined ) { %>
            <form method="post" action="?">
                <table>
                    <tbody>
                        <tr>
                            <td><input type="text" name="email" id="email" placeholder="Email"></td></tr>
                            <tr><td><input type="password" name="password" id="password" placeholder="Mot de Passe"></td></tr>
                            <tr><td><button type="submit">Connexion</button></td></tr>
                        </tr>
                    </tbody>
                </table>
            </form>
        <% } else { %>
            Vous êtes déjà connecté en tant que <%=session.nickname %>
            <p>
                <a href="/disconnect"><button>Déconnexion</button></a>
            </p>
        <% } %>

    <p>Mot de passe oublié ?</p>

</div>

<%- include('footer') %>
